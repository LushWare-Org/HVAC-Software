{{#if approvedAt}}
<div class="approval-block">
  <div class="approval-title">Customer approval recorded</div>
  <div class="approval-detail">Approved by <strong>{{approvedByName}}</strong> ({{approvedByEmail}}) on {{approvedAtFmt}}.</div>
</div>
{{/if}}
