import React from 'react'
import { PostFeed } from '@/components/PostFeed'

export default function Offers() {
  return <PostFeed type="OFFER" title="Offers" emptyText="No offers right now. Check back soon." />
}
